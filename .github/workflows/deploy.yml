name: Deploy tema para o WordPress

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-wordpress
  cancel-in-progress: false

jobs:
  deploy:
    name: Enviar tema via FTP
    runs-on: ubuntu-latest
    environment: espanol

    steps:
      - name: Baixar o repositorio
        uses: actions/checkout@v4

      - name: Validar sintaxe PHP
        run: |
          find espanol -name '*.php' -print0 | xargs -0 -n1 php -l

      - name: Verificar secrets
        run: |
          [ -n "${{ secrets.FTP_SERVER }}" ] && echo "FTP_SERVER: OK" || echo "FTP_SERVER: VAZIO"
          [ -n "${{ secrets.FTP_PORT }}" ] && echo "FTP_PORT: OK" || echo "FTP_PORT: VAZIO"
          [ -n "${{ secrets.FTP_USERNAME }}" ] && echo "FTP_USERNAME: OK" || echo "FTP_USERNAME: VAZIO"
          [ -n "${{ secrets.FTP_PASSWORD }}" ] && echo "FTP_PASSWORD: OK" || echo "FTP_PASSWORD: VAZIO"
          [ -n "${{ secrets.FTP_REMOTE_DIR }}" ] && echo "FTP_REMOTE_DIR: OK" || echo "FTP_REMOTE_DIR: VAZIO"

      - name: Enviar arquivos para o servidor
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          port: ${{ secrets.FTP_PORT }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          log-level: verbose
          local-dir: ./espanol/
          server-dir: ${{ secrets.FTP_REMOTE_DIR }}
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            **/.distignore
            **/.gitignore
            **/README.md
            **/*.map
            **/.DS_Store
            **/Thumbs.db
